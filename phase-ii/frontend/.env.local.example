# Todo Full-Stack Web Application (Phase-2) - Frontend Environment Variables

# Application URL (for Better Auth redirects)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Authentication Secret
# CRITICAL: Must match BETTER_AUTH_SECRET in backend/.env
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-key-here-minimum-32-characters-change-this-in-production

# Database URL (Better Auth needs it for user storage)
# Same as backend DATABASE_URL - use Neon PostgreSQL connection string
DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require

# Application Settings
NEXT_PUBLIC_APP_NAME=Todo Full-Stack App
NEXT_PUBLIC_APP_VERSION=1.0.0

# Node Environment
NODE_ENV=development
