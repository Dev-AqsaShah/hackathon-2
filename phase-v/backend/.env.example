# Todo AI Chatbot (Phase-3) - Backend Environment Variables

# Database Configuration
# Replace with your Neon PostgreSQL connection string
# Async URL (for FastAPI async endpoints)
# Format: postgresql+asyncpg://user:password@host/database?ssl=require
DATABASE_URL=postgresql+asyncpg://user:password@host/database?ssl=require

# Sync URL (for synchronous operations like OpenAI tool execution)
# Format: postgresql://user:password@host/database?sslmode=require
SYNC_DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# OpenAI API Key (for Agents SDK - Phase 3)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Authentication Secret
# CRITICAL: Must match BETTER_AUTH_SECRET in frontend/.env.local
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-key-here-minimum-32-characters

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000

# Frontend URL
FRONTEND_URL=http://localhost:3000

# Application Settings
APP_NAME=Todo AI Chatbot
APP_VERSION=3.0.0
DEBUG=True
